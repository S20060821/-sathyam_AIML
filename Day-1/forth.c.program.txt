#include <stdio.h>
int main() {
    int num;
    printf("enter an integer");
    scanf("%d",&num);
    if(num%3==0&&num%5==0)
    {
         printf("%d is the multiple of 3 and 5");
    }
    else{
    }
     printf("%d is not multiple of 3 and 5");
    
}
